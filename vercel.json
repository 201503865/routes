{
  "version": 2,
  "builds": [
    {
      "src": "build.gradle",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/libs"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/$1" }
  ],
  "build": {
    "env": {
      "JAVA_HOME": "/opt/java/openjdk"
    }
  },
  "buildCommand": "curl -s \"https://get.sdkman.io\" | bash && source \"$HOME/.sdkman/bin/sdkman-init.sh\" && sdk install java 17.0.7-tem && chmod +x gradlew && ./gradlew clean build -x test"
}